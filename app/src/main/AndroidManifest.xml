<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jason.route_application_kotlin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RouteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAycv4bRa_NI4gl7WwkgLGs4EDhn44G8DY" />

        <activity android:name=".features.routeInput.RouteInputActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".features.route.RouteActivity" />
        <activity android:name=".features.addressDetails.AddressDetailsActivity" />
        <activity android:name=".features.CommentInput.CommentInputActivity" />
        <activity android:name=".features.CommentDisplay.CommentDisplayActivity" />
        <activity android:name=".features.correctInvalidAddresses.CorrectInvalidAddressesActivity"></activity>
    </application>

</manifest>